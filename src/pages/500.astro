---
import Layout from "@/layouts/Layout.astro";
import { getContent } from "@/lib/content";
import Button from "@/components/starwind/button/Button.astro";

const content = getContent("de");
const { serverError } = content.errors;
---

<Layout locale="de">
	<section class="py-12 sm:py-16 md:py-20 lg:py-24">
		<div class="mx-auto max-w-3xl px-4 sm:px-6 lg:px-8 text-center">
			<!-- Error Icon -->
			<div class="mb-8">
				<div class="mx-auto flex size-20 items-center justify-center rounded-full bg-muted">
					<span class="text-4xl font-bold text-muted-foreground">500</span>
				</div>
			</div>

			<h1 class="text-3xl sm:text-4xl md:text-5xl font-bold leading-tight text-foreground mb-6">
				{serverError.headline}
			</h1>

			<p class="text-lg sm:text-xl text-muted-foreground leading-relaxed mb-10 max-w-2xl mx-auto">
				{serverError.message}
			</p>

			<Button variant="primary" size="lg" href={serverError.ctaHref}>
				{serverError.ctaLabel}
			</Button>
		</div>
	</section>
</Layout>
